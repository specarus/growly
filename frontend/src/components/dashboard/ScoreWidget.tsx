import { Card } from "../ui/card";

const ScoreWidget = () => {
  return (
    <Card className="shadow-sm border-gray-50 bg-secondary h-full">
      Score Widget
    </Card>
  );
};

export default ScoreWidget;
